/*!
 * UBS Contacts JavaScript Functions
 * Copyright 2015 Unifiedg Building Sciences, Inc.
 */

function get_new_location_id(num_locs) {
	var new_id = parseInt(num_locs) + 1;
	return new_id
}

function get_url(head, tail) {
	return head + tail
}
